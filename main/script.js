'use strict';

// const thumbnails = document.querySelectorAll('.thumbnails-col');

// let currSlide = 0;
// const maxSlide = thumbnails.length - 1;

// const expandPicture = function (thumbnailSrc) {
//   var expandImg = document.getElementById('expandedImg');

//   expandImg.src = thumbnailSrc;
// };

// expandPicture(thumbnails[0].firstChild.src);

// thumbnails.forEach((thumbnail) =>
//   thumbnail.addEventListener('click', expandPicture(thumbnail))
// );
